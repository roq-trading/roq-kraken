{% set version = environ.get('GIT_DESCRIBE_TAG', '0.0.0') %}
{% set number = environ.get('GIT_DESCRIBE_NUMBER', 0) %}

package:
  name: roq-kraken
  version: {{ version }}

source:
  git_url: ..

build:
  skip: True  # [win]
  number: {{ number }}
  run_exports:
    - {{ pin_subpackage('roq-kraken', max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - clangdev
    - cmake
    - make
  host:
    - benchmark
    - roq-api =={{ version }}
    - roq-core =={{ version }}
    - roq-oss-date
    - roq-oss-http-parser
    - roq-server =={{ version }}

about:
  home: https://roq-trading.com
  doc_url: https://roq-trading.com/docs
  dev_url: https://github.com/roq-trading
  license: Commercial
  license_family: PROPRIETARY
  license_file: LICENSE
  summary: Roq Kraken Gateway
